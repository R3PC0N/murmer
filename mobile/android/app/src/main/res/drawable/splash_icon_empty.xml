<?xml version="1.0" encoding="utf-8"?>
<!-- Intentionally empty vector — used as windowSplashScreenAnimatedIcon
     to suppress Android 12+'s default behaviour of showing the launcher
     icon on the native splash screen. The Flutter SplashScreen draws the
     animated waveform instead. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108"/>
