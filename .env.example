# Optional: add your Anthropic API key to enable AI text cleanup (Claude Haiku).
# Get one at https://console.anthropic.com
# Leave empty to use Murmer without AI cleanup (raw Whisper transcription only).
ANTHROPIC_API_KEY=

# Set to false to force-disable AI cleanup even if a key is provided
AI_CLEANUP_ENABLED=true
